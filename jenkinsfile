pipeline {
    agent none

    stages {
        stage('Build') {
            steps {
                echo 'Building..'                
                script{
                    sh 'docker build -t testflask:latest .'
                    sh 'docker run -d -p 8000:8000 testflask:latest'
                    }
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'docker exec testflask python flasktests.py'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                sh 'docker exec testflask python3 flask.py'
            }
        }
    }
